#ifndef ENUMS_H
#define ENUMS_H

enum fuente
{
    COMFORTAA_LIGHT,
    TIMES_NEW_ROMAN,
};

typedef enum fuente Fuente;

enum etapa
{
    MENU_PRINCIPAL,
    MENU_NIVELES,
    INSTRUCCIONES,
    RANKING,
    JUEGO_PRINCIPAL,
};

typedef enum etapa Etapa;

#endif  // ENUMS_H