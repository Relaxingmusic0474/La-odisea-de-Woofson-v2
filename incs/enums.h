#ifndef ENUMS_H
#define ENUMS_H

typedef enum fuente Fuente;
typedef enum etapa Etapa;

enum fuente
{
    COMFORTAA_LIGHT,
    TIMES_NEW_ROMAN,
};

enum etapa
{
    MENU_PRINCIPAL,
    MENU_NIVELES,
    INSTRUCCIONES,
    JUEGO_PRINCIPAL,
    RANKING
};

#endif  // ENUMS_H